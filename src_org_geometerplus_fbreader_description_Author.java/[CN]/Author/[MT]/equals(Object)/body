{
  if (o == this) {
    return true;
  }
  if (!(o instanceof Author)) {
    return false;
  }
  Author a=(Author)o;
  return getSortKey().equals(a.getSortKey()) && getDisplayName().equals(a.getDisplayName());
}
