{
  final byte kind=(byte)myParagraphInfos.get(index);
  return (kind == ZLTextParagraph.Kind.TEXT_PARAGRAPH) ? new ZLTextParagraphImpl(this,index) : new ZLTextSpecialParagraphImpl(kind,this,index);
}
