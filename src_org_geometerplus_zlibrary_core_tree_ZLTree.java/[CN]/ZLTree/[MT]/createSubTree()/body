{
  if (mySubTrees == null) {
    mySubTrees=new ArrayList<T>();
  }
  T tree=createChild();
  mySubTrees.add(tree);
  for (ZLTree parent=this; parent != null; parent=parent.myParent) {
    ++parent.mySize;
  }
  return tree;
}
