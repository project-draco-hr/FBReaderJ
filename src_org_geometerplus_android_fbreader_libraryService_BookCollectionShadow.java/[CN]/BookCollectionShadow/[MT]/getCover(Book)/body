{
  if (myInterface == null) {
    return null;
  }
  try {
    return myInterface.getCover(SerializerUtil.serialize(book));
  }
 catch (  RemoteException e) {
    e.printStackTrace();
    return null;
  }
}
