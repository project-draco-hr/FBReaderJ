{
  if (myInterface == null) {
    return Collections.emptyList();
  }
  try {
    return SerializerUtil.deserializeBookList(myInterface.booksForSeriesAndAuthor(series,Util.authorToString(author)));
  }
 catch (  RemoteException e) {
    return Collections.emptyList();
  }
}
