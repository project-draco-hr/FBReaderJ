{
  if (myInterface == null) {
    return null;
  }
  try {
    return myInterface.getCover(SerializerUtil.serialize(book),maxWidth,maxHeight);
  }
 catch (  RemoteException e) {
    e.printStackTrace();
    return null;
  }
}
