{
  if (myInterface == null) {
    return null;
  }
  try {
    return myInterface.getCover(SerializerUtil.serialize(book),width,height);
  }
 catch (  RemoteException e) {
    e.printStackTrace();
    return null;
  }
}
