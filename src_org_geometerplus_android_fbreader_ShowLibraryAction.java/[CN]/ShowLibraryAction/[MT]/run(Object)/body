{
  try {
    startLibraryActivity(new Intent("android.fbreader.action.EXTERNAL_LIBRARY"));
  }
 catch (  ActivityNotFoundException e) {
    startLibraryActivity(new Intent(BaseActivity.getApplicationContext(),LibraryActivity.class));
  }
}
