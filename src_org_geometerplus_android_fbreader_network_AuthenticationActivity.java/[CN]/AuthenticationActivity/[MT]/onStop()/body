{
  final ZLNetworkManager.CredentialsCreator creator=ZLNetworkManager.Instance().getCredentialsCreator();
  if (creator instanceof CredentialsCreator) {
    ((CredentialsCreator)creator).release();
  }
  super.onStop();
}
