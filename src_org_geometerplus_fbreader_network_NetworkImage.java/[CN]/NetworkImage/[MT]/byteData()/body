{
  if (!isSynchronized()) {
    return null;
  }
  final String path=getFilePath();
  if (path == null) {
    return null;
  }
  return new ZLFileImage(mimeType(),ZLFile.createFileByPath(path)).byteData();
}
