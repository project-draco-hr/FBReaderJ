{
  if (this == other) {
    return true;
  }
  if (!(other instanceof FileEncryptionInfo)) {
    return false;
  }
  final FileEncryptionInfo oInfo=(FileEncryptionInfo)other;
  return ComparisonUtil.equal(Uri,oInfo.Uri) && ComparisonUtil.equal(Method,oInfo.Method) && ComparisonUtil.equal(Algorithm,oInfo.Algorithm)&& ComparisonUtil.equal(ContentId,oInfo.ContentId);
}
