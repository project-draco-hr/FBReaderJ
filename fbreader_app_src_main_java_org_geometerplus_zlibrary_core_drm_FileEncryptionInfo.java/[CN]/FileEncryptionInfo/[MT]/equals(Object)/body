{
  if (this == other) {
    return true;
  }
  if (!(other instanceof FileEncryptionInfo)) {
    return false;
  }
  final FileEncryptionInfo oInfo=(FileEncryptionInfo)other;
  return MiscUtil.equals(Uri,oInfo.Uri) && MiscUtil.equals(Method,oInfo.Method) && MiscUtil.equals(Algorithm,oInfo.Algorithm)&& MiscUtil.equals(ContentId,oInfo.ContentId);
}
