{
  return new ZLImageFileProxy(file){
    @Override protected ZLImage retrieveRealImage(){
      final ZLImage[] box=new ZLImage[1];
      readCoverInternal(File,box);
      return box[0];
    }
  }
;
}
