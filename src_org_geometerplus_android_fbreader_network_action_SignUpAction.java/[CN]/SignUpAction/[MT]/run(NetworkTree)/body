{
  final INetworkLink link=tree.getLink();
  try {
    myActivity.startActivity(Util.authorisationIntent(link,myActivity,UserRegistrationActivity.class));
  }
 catch (  ActivityNotFoundException e) {
  }
}
