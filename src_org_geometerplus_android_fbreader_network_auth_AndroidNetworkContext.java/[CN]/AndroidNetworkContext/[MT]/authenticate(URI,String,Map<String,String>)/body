{
  if (!"https".equalsIgnoreCase(uri.getScheme())) {
    return Collections.singletonMap("error","Connection is not secure");
  }
  return GooglePlayServicesUtil.isGooglePlayServicesAvailable(getContext()) == ConnectionResult.SUCCESS ? authenticateToken(uri,realm,params) : authenticateWeb(uri,realm,params);
}
