{
  int length=0;
  for (  char[] part : data) {
    length+=part.length;
  }
  myData=new char[length];
  int pos=0;
  for (  char[] part : data) {
    System.arraycopy(part,0,myData,pos,part.length);
    pos+=part.length;
  }
}
