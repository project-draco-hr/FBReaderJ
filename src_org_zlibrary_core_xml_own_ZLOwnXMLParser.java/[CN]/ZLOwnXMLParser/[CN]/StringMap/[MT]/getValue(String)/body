{
  final int size=mySize;
  if (size > 0) {
    key=key.intern();
    final String[] keys=myKeys;
    for (int i=0; i < size; ++i) {
      if (key == keys[i]) {
        return myValues[i];
      }
    }
  }
  return null;
}
