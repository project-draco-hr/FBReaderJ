{
  CharSequence s=mySequence;
  CharSequence s1=string.mySequence;
  int len=s.length();
  int len1=s1.length();
  if (len != len1) {
    return len - len1;
  }
  for (int i=0; i < len; ++i) {
    int c=s.charAt(i);
    int c1=s1.charAt(i);
    if (c != c1) {
      return c - c1;
    }
  }
  return 0;
}
