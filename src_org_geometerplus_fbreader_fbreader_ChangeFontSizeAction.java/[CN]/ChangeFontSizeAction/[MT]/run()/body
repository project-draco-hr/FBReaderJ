{
  ZLIntegerRangeOption option=ZLTextStyleCollection.getInstance().getBaseStyle().FontSizeOption;
  option.setValue(option.getValue() + myDelta);
  Reader.clearTextCaches();
  Reader.refreshWindow();
}
