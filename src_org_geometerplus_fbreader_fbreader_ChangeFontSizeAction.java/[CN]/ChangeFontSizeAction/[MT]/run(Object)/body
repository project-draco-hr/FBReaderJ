{
  final ZLIntegerRangeOption option=Reader.TextStyleCollection.getBaseStyle().FontSizeOption;
  option.setValue(option.getValue() + myDelta);
  Reader.clearTextCaches();
  Reader.getViewWidget().repaint();
}
