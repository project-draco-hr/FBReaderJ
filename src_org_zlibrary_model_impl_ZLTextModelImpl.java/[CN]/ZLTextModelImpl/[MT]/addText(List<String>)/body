{
  ZLTextParagraph paragraph=myParagraphs.get(myParagraphs.size() - 1);
  StringBuilder sb=new StringBuilder();
  for (  String str : text)   sb.append(str);
  paragraph.addEntry(new ZLTextEntryImpl(sb.toString()));
}
