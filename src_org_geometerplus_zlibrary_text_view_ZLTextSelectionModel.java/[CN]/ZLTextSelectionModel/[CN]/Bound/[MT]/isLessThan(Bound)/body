{
  if (!Before.Exists) {
    return true;
  }
  if (!bound.Before.Exists) {
    return false;
  }
  if (!After.Exists) {
    return false;
  }
  if (!bound.After.Exists) {
    return true;
  }
  return Before.compareTo(bound.Before) < 0;
}
