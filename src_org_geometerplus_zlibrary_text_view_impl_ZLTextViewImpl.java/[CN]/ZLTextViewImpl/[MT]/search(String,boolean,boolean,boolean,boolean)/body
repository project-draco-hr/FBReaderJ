{
  if (text.length() == 0) {
    return;
  }
  int startIndex=0;
  int endIndex=myModel.getParagraphsNumber();
  if (thisSectionOnly) {
  }
  myModel.search(text,startIndex,endIndex,ignoreCase);
  if (!StartCursor.isNull()) {
    rebuildPaintInfo(true);
    ZLTextMark position=StartCursor.getPosition();
    gotoMark(wholeText ? (backward ? myModel.getLastMark() : myModel.getFirstMark()) : (backward ? myModel.getPreviousMark(position) : myModel.getNextMark(position)));
    ZLApplication.Instance().refreshWindow();
  }
}
