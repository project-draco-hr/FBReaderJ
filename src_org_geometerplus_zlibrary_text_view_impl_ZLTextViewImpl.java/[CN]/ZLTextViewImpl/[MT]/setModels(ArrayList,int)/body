{
  myModelList=(models != null) ? models : new ArrayList();
  myModel=(current >= 0 && current < myModelList.size()) ? (ZLTextModel)myModelList.get(current) : null;
  myCurrentModelIndex=current;
  setModelInternal();
}
