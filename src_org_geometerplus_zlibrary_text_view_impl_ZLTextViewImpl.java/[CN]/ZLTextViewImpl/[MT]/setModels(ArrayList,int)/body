{
  System.out.println(current);
  myModels=(models != null) ? models : new ArrayList();
  myModel=(current >= 0 && current < myModels.size()) ? (ZLTextModel)myModels.get(current) : null;
  myCurrentModelIndex=current;
  setModelInternal();
}
