{
  final Item item=getListAdapter().getItem(position);
  if (item instanceof ServiceInfoItem) {
    try {
      startActivity(new Intent(Intent.ACTION_VIEW,((ServiceInfoItem)item).URI,getApplicationContext(),AddCustomCatalogActivity.class));
      finish();
    }
 catch (    ActivityNotFoundException e) {
    }
  }
}
