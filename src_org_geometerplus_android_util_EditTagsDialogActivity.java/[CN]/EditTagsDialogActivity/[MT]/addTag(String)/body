{
  if (tag.length() != 0) {
    String[] tags=tag.split(",");
    for (    String s : tags) {
      s=s.trim();
      if (!myEditList.contains(s)) {
        myEditList.add(s);
      }
    }
    ((BaseAdapter)getListAdapter()).notifyDataSetChanged();
  }
}
