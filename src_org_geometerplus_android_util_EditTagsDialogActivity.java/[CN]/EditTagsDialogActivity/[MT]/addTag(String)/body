{
  if (tag.length() != 0) {
    String[] tags=tag.split(",");
    for (    String s : tags) {
      s=s.trim();
      if (!myTagList.contains(s)) {
        myTagList.add(s);
      }
    }
    ((TagsAdapter)getListAdapter()).notifyDataSetChanged();
  }
}
