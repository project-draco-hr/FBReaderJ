{
  String value=myDeltaConfig.getValue(group.intern(),name.intern(),defaultValue.intern());
  if (value.intern() == defaultValue.intern()) {
    value=myMainConfig.getValue(group.intern(),name.intern(),defaultValue.intern());
  }
  return value.intern();
}
