{
  try {
    decode();
    final File file=new File(decodedFileName());
    return "imagefile://" + decodedFileName() + "\0000\000"+ (int)file.length();
  }
 catch (  Exception e) {
    return null;
  }
}
