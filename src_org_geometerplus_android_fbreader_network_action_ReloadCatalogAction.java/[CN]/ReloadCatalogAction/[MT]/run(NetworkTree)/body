{
  if (ItemsLoadingService.getRunnable(tree) != null) {
    return;
  }
  NetworkCatalogActions.clearTree(myActivity,(NetworkCatalogTree)tree);
  ItemsLoadingService.start(myActivity,tree,new NetworkCatalogActions.CatalogExpander(myActivity,(NetworkCatalogTree)tree,false,false));
}
