{
  final String id=ScrollingPreferences.Instance().TapZonesSchemeOption.getValue().toString();
  if (!id.equals(myZoneMapId)) {
    myZoneMap=new TapZoneMap(id);
    myZoneMapId=id;
  }
  return myZoneMap;
}
