{
  final String id=ScrollingPreferences.Instance().HorizontalOption.getValue() ? "right_to_left" : "up";
  if (id != myZoneMapId) {
    myZoneMap=new TapZoneMap(id);
    myZoneMapId=id;
  }
  return myZoneMap;
}
