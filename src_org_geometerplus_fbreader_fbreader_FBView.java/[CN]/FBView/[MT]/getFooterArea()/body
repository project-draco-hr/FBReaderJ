{
  if (scrollbarType() == SCROLLBAR_SHOW_AS_FOOTER) {
    if (myFooter == null) {
      myFooter=new Footer();
    }
  }
 else {
    if (myFooter != null) {
      myFooter=null;
    }
  }
  return myFooter;
}
