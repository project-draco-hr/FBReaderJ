{
  if (myParagraphIndex >= myMaxParagraphIndex) {
    stopTalking();
    return;
  }
  String s=lookForValidParagraphString(direction);
  if (show) {
    showString(s);
  }
  if (speak) {
    speakString(s);
  }
}
