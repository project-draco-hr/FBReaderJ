{
  if (myParagraphIndex >= myParagraphsNumber - 1) {
    stopTalking();
    return;
  }
  String s=lookForValidParagraphString(direction);
  if (show) {
    showString(s);
  }
  if (speak) {
    speakString(s);
  }
}
