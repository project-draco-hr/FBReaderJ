{
switch (event) {
case Added:
    return Title.equals(TitleUtil.firstTitleLetter(book)) && createBookWithAuthorsSubTree(book);
case Removed:
  return false;
default :
case Updated:
return false;
}
}
