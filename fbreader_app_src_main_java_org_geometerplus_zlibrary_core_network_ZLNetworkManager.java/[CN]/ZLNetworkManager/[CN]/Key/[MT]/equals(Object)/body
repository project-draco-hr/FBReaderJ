{
  if (o == this) {
    return true;
  }
  if (!(o instanceof Key)) {
    return false;
  }
  final Key k=(Key)o;
  return ComparisonUtil.equal(Domain,k.Domain) && ComparisonUtil.equal(Path,k.Path) && ComparisonUtil.equal(Name,k.Name);
}
