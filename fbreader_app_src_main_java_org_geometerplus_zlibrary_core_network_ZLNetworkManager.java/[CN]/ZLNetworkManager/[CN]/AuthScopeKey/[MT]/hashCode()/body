{
  if (myScope == null) {
    return 0;
  }
  return myScope.getPort() + ComparisonUtil.hashCode(myScope.getHost()) + ComparisonUtil.hashCode(myScope.getScheme())+ ComparisonUtil.hashCode(myScope.getRealm());
}
