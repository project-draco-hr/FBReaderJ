{
  final StringBuilder buffer=new StringBuilder();
  final ZLColor bgColor=style.getBackgroundColor();
  appendTag(buffer,"style",true,"id",String.valueOf(style.Id),"name",style.getName(),"bg-color",bgColor != null ? String.valueOf(bgColor.intValue()) : "-1");
  return buffer.toString();
}
