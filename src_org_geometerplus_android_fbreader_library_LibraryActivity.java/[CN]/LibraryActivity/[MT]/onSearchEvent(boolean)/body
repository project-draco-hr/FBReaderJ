{
  runOnUiThread(new Runnable(){
    public void run(){
      if (found) {
        openSearchResults();
      }
 else {
        UIMessageUtil.showErrorMessage(LibraryActivity.this,"bookNotFound");
      }
    }
  }
);
}
