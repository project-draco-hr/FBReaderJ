{
  try {
    final ZLPhysicalFile file=book.File.getPhysicalFile();
    if (file == null) {
      return;
    }
    startActivity(new Intent(Intent.ACTION_SEND).setType(FileTypeCollection.Instance.mimeType(book.File).Name).putExtra(Intent.EXTRA_STREAM,Uri.fromFile(file.javaFile())));
  }
 catch (  ActivityNotFoundException e) {
  }
}
