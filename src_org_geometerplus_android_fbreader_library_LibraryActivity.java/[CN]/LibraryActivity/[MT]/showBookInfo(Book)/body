{
  OrientationUtil.startActivityForResult(this,new Intent(getApplicationContext(),BookInfoActivity.class).putExtra(BookInfoActivity.CURRENT_BOOK_KEY,SerializerUtil.serialize(book)),BOOK_INFO_REQUEST);
}
