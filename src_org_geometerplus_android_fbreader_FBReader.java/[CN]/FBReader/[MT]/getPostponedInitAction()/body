{
  return new Runnable(){
    public void run(){
      runOnUiThread(new Runnable(){
        public void run(){
          initPluginActions();
          new TipRunner().start();
          DictionaryUtil.init(FBReader.this);
        }
      }
);
    }
  }
;
}
