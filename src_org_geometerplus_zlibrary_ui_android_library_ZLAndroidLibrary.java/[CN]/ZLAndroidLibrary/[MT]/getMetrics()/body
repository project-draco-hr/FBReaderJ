{
  if (myMetrics == null) {
    if (myActivity == null) {
      return null;
    }
    myMetrics=new DisplayMetrics();
    myActivity.getWindowManager().getDefaultDisplay().getMetrics(myMetrics);
  }
  return myMetrics;
}
