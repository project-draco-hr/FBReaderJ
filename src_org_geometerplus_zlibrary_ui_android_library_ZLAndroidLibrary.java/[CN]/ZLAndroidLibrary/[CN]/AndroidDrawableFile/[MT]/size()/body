{
  try {
    AssetFileDescriptor descriptor=myApplication.getResources().openRawResourceFd(myId);
    long length=descriptor.getLength();
    descriptor.close();
    return length;
  }
 catch (  IOException e) {
    return 0;
  }
catch (  Resources.NotFoundException e) {
    return 0;
  }
}
