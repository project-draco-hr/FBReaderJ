{
  try {
    AssetFileDescriptor descriptor=myApplication.getAssets().openFd(getPath());
    if (descriptor == null) {
      return false;
    }
    descriptor.close();
    return true;
  }
 catch (  IOException e) {
    return false;
  }
}
