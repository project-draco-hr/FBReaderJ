{
  final NetworkView nView=NetworkView.Instance();
  return nView != null && nView.isInitialized() && nView.containsItemsLoadingRunnable(NetworkLibrary.Instance().getSearchItemTree().getUniqueKey());
}
