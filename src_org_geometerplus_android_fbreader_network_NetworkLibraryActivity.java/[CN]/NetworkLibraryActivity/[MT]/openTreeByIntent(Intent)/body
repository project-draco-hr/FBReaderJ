{
  if (OPEN_CATALOG_ACTION.equals(intent.getAction())) {
    final NetworkTree tree=NetworkLibrary.Instance().getCatalogTreeByUrl(intent.getData().toString());
    if (tree != null) {
      new OpenCatalogAction(this).run(tree);
      return true;
    }
  }
  return false;
}
