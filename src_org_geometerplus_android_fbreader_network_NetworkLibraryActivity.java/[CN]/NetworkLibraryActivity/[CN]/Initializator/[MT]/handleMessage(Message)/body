{
  if (message.what == 0) {
    runInitialization();
  }
 else   if (message.obj == null) {
    startService(new Intent(getApplicationContext(),LibraryInitializationService.class));
    prepareView();
  }
 else {
    processResults((String)message.obj);
  }
}
