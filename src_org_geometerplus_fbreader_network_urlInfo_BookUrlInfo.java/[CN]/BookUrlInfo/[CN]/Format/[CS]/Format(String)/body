{
  Extension=extension;
  int priority=0;
  for (  Format f : Arrays.asList(NONE,MOBIPOCKET,FB2,FB2_ZIP,EPUB)) {
    if (f.equals(this)) {
      priority=f.myPriority;
      break;
    }
  }
  myPriority=priority;
}
