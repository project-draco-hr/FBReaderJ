{
  try {
    return client.execute(request,context);
  }
 catch (  BearerAuthenticationException e) {
    if (myBearerAuthenticator != null && myBearerAuthenticator.authenticate(request.getURI(),e.Params)) {
      return client.execute(request,context);
    }
    throw e;
  }
}
