{
  if (o == this) {
    return true;
  }
  if (!(o instanceof Key)) {
    return false;
  }
  final Key k=(Key)o;
  return MiscUtil.equals(Domain,k.Domain) && MiscUtil.equals(Path,k.Path) && MiscUtil.equals(Name,k.Name);
}
