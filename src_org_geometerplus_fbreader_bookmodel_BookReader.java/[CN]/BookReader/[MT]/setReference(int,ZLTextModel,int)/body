{
  final ContentsTree contentsTree=Model.ContentsTree;
  if (contentsParagraphNumber < contentsTree.getSize()) {
    contentsTree.setReference(contentsTree.getTree(contentsParagraphNumber),textModel,referenceNumber);
  }
}
