{
  clear();
  myLeftMostRegion=myView.findRegion(x,y,ZLTextView.MAX_SELECTION_DISTANCE,ZLTextRegion.AnyRegionFilter);
  if (myLeftMostRegion == null) {
    return false;
  }
  myRightMostRegion=myLeftMostRegion;
  return true;
}
