{
  clear();
  final ZLTextRegion initialRegion=myView.findRegion(x,y,ZLTextView.MAX_SELECTION_DISTANCE,ZLTextRegion.AnyRegionFilter);
  if (initialRegion == null) {
    return false;
  }
  myLeftBound=initialRegion.getFirstArea();
  myRightBound=initialRegion.getLastArea();
  return true;
}
