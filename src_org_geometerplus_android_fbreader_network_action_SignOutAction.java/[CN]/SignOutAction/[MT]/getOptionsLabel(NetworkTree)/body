{
  final NetworkAuthenticationManager mgr=(((NetworkCatalogRootTree)tree).Item).Link.authenticationManager();
  final String userName=mgr != null && mgr.mayBeAuthorised(false) ? mgr.currentUserName() : "";
  return super.getOptionsLabel(tree).replace("%s",userName);
}
