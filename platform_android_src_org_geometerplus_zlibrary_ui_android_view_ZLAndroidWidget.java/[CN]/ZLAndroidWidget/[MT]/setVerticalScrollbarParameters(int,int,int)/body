{
  if (full < 0) {
    full=0;
  }
  if (from < 0) {
    from=0;
  }
 else   if (from >= full) {
    from=full - 1;
  }
  if (to <= from) {
    to=from + 1;
  }
 else   if (to > full) {
    to=full;
  }
  myScrollBarRange=full;
  myScrollBarOffset=from;
  myScrollBarThumbSize=to - from;
}
