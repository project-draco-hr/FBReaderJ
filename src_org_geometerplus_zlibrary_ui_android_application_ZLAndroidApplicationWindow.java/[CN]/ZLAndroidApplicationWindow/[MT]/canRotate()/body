{
  final Activity activity=((ZLAndroidLibrary)ZLAndroidLibrary.Instance()).getActivity();
  return activity != null && !((ZLAndroidApplication)activity.getApplication()).AutoOrientationOption.getValue();
}
