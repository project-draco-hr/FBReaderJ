{
  if (myFile == null)   myParent.runOnUiThread(myReturnRes);
  for (  ZLFile file : myFile.children()) {
    if (!Thread.currentThread().isInterrupted()) {
      if (file.isDirectory() || file.isArchive())       myOrders.add(new FileOrder(file));
 else       if (PluginCollection.Instance().getPlugin(file) != null)       myOrders.add(new FileOrder(file));
      myParent.runOnUiThread(myReturnRes);
    }
  }
  myParent.runOnUiThread(myReturnRes);
}
