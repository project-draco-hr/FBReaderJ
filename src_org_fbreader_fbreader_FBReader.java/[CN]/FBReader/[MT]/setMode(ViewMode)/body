{
  if (mode == myMode) {
    return;
  }
  myPreviousMode=myMode;
  myMode=mode;
switch (mode) {
case BOOK_TEXT:
    setView(myBookTextView);
  break;
case CONTENTS:
setView(myContentsView);
break;
default :
break;
}
}
