{
  if ("Serif".equals(fontFamily)) {
    return "serif";
  }
  if ("sans-serif".equals(fontFamily) || "serif".equals(fontFamily) || "monospace".equals(fontFamily)) {
    return fontFamily;
  }
  return "sans-serif";
}
