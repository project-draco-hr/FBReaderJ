{
  if (ourFontMap == null) {
    ourFontMap=new ZLTTFInfoDetector().collectFonts(new File("/sdcard/fonts").listFiles(new FilenameFilter(){
      public boolean accept(      File dir,      String name){
        return name.endsWith(".ttf") && !name.startsWith(".");
      }
    }
));
  }
  return ourFontMap;
}
