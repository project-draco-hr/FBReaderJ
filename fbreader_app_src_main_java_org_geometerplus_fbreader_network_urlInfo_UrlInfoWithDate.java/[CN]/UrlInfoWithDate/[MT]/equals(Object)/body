{
  if (o == this) {
    return true;
  }
  if (!(o instanceof UrlInfoWithDate)) {
    return false;
  }
  final UrlInfoWithDate info=(UrlInfoWithDate)o;
  return InfoType == info.InfoType && MiscUtil.equals(Url,info.Url) && MiscUtil.equals(Mime,info.Mime) && MiscUtil.equals(Updated,info.Updated);
}
