{
  if (view == null) {
    return;
  }
  if (getMyViewWidget() != null) {
    getMyViewWidget().setView(view);
    resetWindowCaption();
    refreshWindow();
  }
 else {
    myInitialView=view;
  }
}
