{
  setMyViewWidget(myWindow.createViewWidget());
  myWindow.init();
  setView(myInitialView);
  if (KeyboardControlOption.getValue()) {
    grabAllKeys(true);
  }
  myWindow.init();
  setView(myInitialView);
}
