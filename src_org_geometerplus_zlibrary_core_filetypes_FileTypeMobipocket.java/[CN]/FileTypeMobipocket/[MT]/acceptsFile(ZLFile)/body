{
  if (super.acceptsFile(file)) {
    return true;
  }
  final String extension=file.getExtension();
  return ("mobi".equalsIgnoreCase(extension) || "azw3".equalsIgnoreCase(extension)) && "BOOKMOBI".equals(palmFileType(file));
}
