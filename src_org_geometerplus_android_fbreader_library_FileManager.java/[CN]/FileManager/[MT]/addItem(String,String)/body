{
  final ZLResource resource=Library.resource().getResource(resourceKey);
  getListAdapter().add(new FileItem(myFileItem,ZLFile.createFileByPath(path),resource.getValue(),resource.getResource("summary").getValue()));
}
