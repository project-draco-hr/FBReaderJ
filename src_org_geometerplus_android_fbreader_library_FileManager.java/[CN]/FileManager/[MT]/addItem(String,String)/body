{
  final ZLResource resource=Library.resource().getResource(resourceKey);
  getListAdapter().add(new FileTree(myCurrentTree,ZLFile.createFileByPath(path),resource.getValue(),resource.getResource("summary").getValue()));
}
