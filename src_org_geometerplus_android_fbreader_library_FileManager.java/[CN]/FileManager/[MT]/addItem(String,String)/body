{
  final ZLResource resource=myResource.getResource(resourceKey);
  ((ListAdapter)getListAdapter()).add(new FileItem(ZLFile.createFileByPath(path),resource.getValue(),resource.getResource("summary").getValue()));
}
