{
  if (tag.length() != 0) {
    String[] tags=tag.split(",");
    for (    String s : tags) {
      s=s.trim();
      if (!myAuthorList.contains(s)) {
        myAuthorList.add(s);
      }
    }
    ((AuthorsAdapter)getListAdapter()).notifyDataSetChanged();
  }
}
