{
  return new ZLImageProxy(){
    @Override public String getId(){
      return file.getPath();
    }
    @Override public int sourceType(){
      return SourceType.DISK;
    }
    @Override public ZLImage getRealImage(){
      return readCoverInternal(file);
    }
  }
;
}
