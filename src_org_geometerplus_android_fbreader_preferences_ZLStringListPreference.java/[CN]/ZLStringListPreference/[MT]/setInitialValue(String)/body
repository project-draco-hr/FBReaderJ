{
  if (value == null) {
    return false;
  }
  final int index=findIndexOfValue(value);
  if (index >= 0) {
    setValueIndex(index);
    setSummary(getEntry());
    return true;
  }
  return false;
}
