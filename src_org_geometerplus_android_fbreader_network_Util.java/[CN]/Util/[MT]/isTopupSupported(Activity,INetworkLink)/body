{
  return isTopupSupported(activity,link,BROWSER_ACTION_ID) || isTopupSupported(activity,link,SMS_ACTION_ID) || isTopupSupported(activity,link,CREDIT_CARD_ACTION_ID)|| isTopupSupported(activity,link,SELF_SERVICE_ACTION_ID);
}
