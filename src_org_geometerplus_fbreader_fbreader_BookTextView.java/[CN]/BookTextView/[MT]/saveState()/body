{
  new Thread(new Runnable(){
    public void run(){
      ZLConfig.Instance().executeAsATransaction(new Runnable(){
        public void run(){
          saveStateInternal();
        }
      }
);
    }
  }
).start();
}
