{
  final FBView fbView=getTextView();
  final Bookmark bookmark=new Bookmark(Collection,Model.Book,fbView.getModel().getId(),fbView.getSelectedSnippet(),true);
  Collection.saveBookmark(bookmark);
  fbView.clearSelection();
  return bookmark;
}
