{
  if (author == null) {
    return;
  }
  if (myAuthors == null) {
    myAuthors=new ArrayList<Author>();
    myAuthors.add(author);
    myIsChanged=true;
  }
 else {
    if (!myAuthors.contains(author)) {
      myAuthors.add(author);
      myIsChanged=true;
    }
  }
}
