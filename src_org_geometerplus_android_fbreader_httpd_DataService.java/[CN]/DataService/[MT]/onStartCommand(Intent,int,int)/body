{
  new Thread(new Runnable(){
    public void run(){
      try {
        myServer=new DataServer();
        myServer.start();
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
).start();
  return START_STICKY;
}
