{
  if (ZLMiscUtil.equals(language,myLanguage)) {
    return;
  }
  myLanguage=language;
  unload();
  if (language != null) {
    new ZLTextHyphenationReader(this).read(ZLResourceFile.createResourceFile("hyphenationPatterns/" + language + ".pattern"));
  }
}
