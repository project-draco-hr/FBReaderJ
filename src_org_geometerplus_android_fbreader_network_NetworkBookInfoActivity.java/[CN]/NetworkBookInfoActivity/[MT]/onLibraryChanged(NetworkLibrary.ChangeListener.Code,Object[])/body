{
  if (code == NetworkLibrary.ChangeListener.Code.InitializationFailed) {
  }
  if (myBook == null) {
    return;
  }
  runOnUiThread(new Runnable(){
    public void run(){
      updateView();
    }
  }
);
}
