{
  ZLTextMetrics m=myMetrics;
  if (m == null) {
    m=new ZLTextMetrics(ZLibrary.Instance().getDisplayDPI(),getTextStyleCollection().getDefaultFontSize(),100,100);
    myMetrics=m;
  }
  return m;
}
