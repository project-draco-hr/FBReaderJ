{
  ZLSwingViewWidget viewWidget=new ZLSwingViewWidget(ZLSwingViewWidget.Angle.getByDegrees(getApplication().AngleStateOption.getValue()));
  myMainPanel.add(viewWidget.getPanel(),BorderLayout.CENTER);
  return viewWidget;
}
