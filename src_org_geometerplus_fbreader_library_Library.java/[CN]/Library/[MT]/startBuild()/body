{
  setStatus(myStatusMask | STATUS_LOADING);
  new Thread("Library.build"){
    public void run(){
      try {
        build();
      }
  finally {
        setStatus(myStatusMask & ~STATUS_LOADING);
      }
    }
  }
.start();
}
