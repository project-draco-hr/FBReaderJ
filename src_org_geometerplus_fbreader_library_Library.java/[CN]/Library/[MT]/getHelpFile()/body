{
  final ZLResourceFile file=ZLResourceFile.createResourceFile("help/MiniHelp." + Locale.getDefault().getLanguage() + ".fb2");
  if (file.exists()) {
    return file;
  }
  return ZLResourceFile.createResourceFile("help/MiniHelp.en.fb2");
}
