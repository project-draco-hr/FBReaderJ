{
  if (tree instanceof BookInSeriesTree) {
    final float difference=Book.getSeriesInfo().Index - ((BookTree)tree).Book.getSeriesInfo().Index;
    if (difference != 0) {
      return difference > 0 ? 1 : -1;
    }
  }
  return super.compareTo(tree);
}
