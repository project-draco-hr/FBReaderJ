{
  if (!myIsSynchronized) {
    String value=myConfig.getValue(myGroup,myOptionName,myDefaultValue.toString());
    myValue=ZLFromStringConverter.getBoolean3Value(value);
    myIsSynchronized=true;
  }
  return myValue;
}
