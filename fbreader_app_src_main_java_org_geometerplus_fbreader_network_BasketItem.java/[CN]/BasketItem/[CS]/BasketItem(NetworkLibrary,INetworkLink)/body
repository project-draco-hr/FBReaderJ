{
  super(link,NetworkLibrary.resource().getResource("basket").getValue(),NetworkLibrary.resource().getResource("basketSummaryEmpty").getValue(),new UrlInfoCollection<UrlInfo>(),Accessibility.ALWAYS,FLAGS_DEFAULT & ~FLAGS_GROUP);
  myLibrary=library;
  myBooksInBasketOption=new ZLStringListOption(Link.getStringId(),"Basket",Collections.<String>emptyList(),",");
}
