{
  super(link,NetworkLibrary.resource().getResource("basket").getValue(),NetworkLibrary.resource().getResource("basketSummaryEmpty").getValue(),urls,Accessibility.ALWAYS,FLAGS_DEFAULT & ~FLAGS_GROUP,null);
  link.setSupportsBasket();
}
