{
  if ("base".equals(tag) && Screen.equals(attributes.getValue("screen"))) {
    myBaseStyle=new ZLTextBaseStyle(Screen,attributes.getValue("family"),intValue(attributes,"fontSize",0));
  }
  return false;
}
