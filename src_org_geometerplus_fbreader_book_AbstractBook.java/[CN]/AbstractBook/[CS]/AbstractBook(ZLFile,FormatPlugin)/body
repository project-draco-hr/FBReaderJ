{
  super(null);
  if (file == null) {
    throw new IllegalArgumentException("Creating book with no file");
  }
  myId=-1;
  File=plugin.realBookFile(file);
  readMetainfo(plugin);
  myIsSaved=false;
}
