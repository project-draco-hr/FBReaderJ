{
  if (this == other) {
    return true;
  }
  if (!(other instanceof Pair)) {
    return false;
  }
  final Pair pair=(Pair)other;
  return ComparisonUtil.equal(First,pair.First) && ComparisonUtil.equal(Second,pair.Second);
}
