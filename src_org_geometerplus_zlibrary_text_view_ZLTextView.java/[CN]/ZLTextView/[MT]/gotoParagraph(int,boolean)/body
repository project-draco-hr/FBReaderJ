{
  if (myModel == null) {
    return;
  }
  if (last) {
    if ((num > 0) && (num <= (int)myModel.getParagraphsNumber())) {
      moveEndCursor(num);
    }
  }
 else {
    if ((num >= 0) && (num < (int)myModel.getParagraphsNumber())) {
      moveStartCursor(num);
    }
  }
}
