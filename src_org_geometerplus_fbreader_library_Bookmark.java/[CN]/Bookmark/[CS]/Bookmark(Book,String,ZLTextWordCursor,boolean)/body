{
  super(cursor);
  myId=-1;
  myBookId=book.getId();
  myBookTitle=book.getTitle();
  myText=createBookmarkText(cursor,20);
  myCreationDate=new Date();
  ModelId=modelId;
  IsVisible=isVisible;
  myIsChanged=true;
}
