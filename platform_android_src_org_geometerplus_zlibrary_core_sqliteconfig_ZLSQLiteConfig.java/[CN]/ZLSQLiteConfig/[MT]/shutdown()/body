{
  if (myDatabase != null) {
    new Thread(new Runnable(){
      public void run(){
synchronized (this) {
          if (myDatabase != null) {
            myDatabase.close();
            myDatabase=null;
          }
        }
      }
    }
).start();
  }
}
