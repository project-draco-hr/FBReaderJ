{
  if (o == this) {
    return true;
  }
  if (!(o instanceof ZLTextMetrics)) {
    return false;
  }
  final ZLTextMetrics oo=(ZLTextMetrics)o;
  return DPI == oo.DPI && FullWidth == oo.FullWidth && FullHeight == oo.FullHeight;
}
