{
  if (visible) {
    if (!myInitialized) {
      createItem();
      myInitialized=true;
    }
    setActive(myOption.isActive());
    show();
  }
 else {
    if (myInitialized) {
      hide();
    }
  }
}
