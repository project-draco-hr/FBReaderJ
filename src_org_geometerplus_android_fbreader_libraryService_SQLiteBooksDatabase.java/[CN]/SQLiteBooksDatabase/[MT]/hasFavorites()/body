{
  final Cursor cursor=myDatabase.rawQuery("SELECT BookLabel.book_id FROM BookLabel" + " INNER JOIN Labels ON BookLabel.label_id=Labels.label_id" + " WHERE Labels.name='favorite' LIMIT 1",null);
  boolean result=cursor.moveToNext();
  cursor.close();
  return result;
}
