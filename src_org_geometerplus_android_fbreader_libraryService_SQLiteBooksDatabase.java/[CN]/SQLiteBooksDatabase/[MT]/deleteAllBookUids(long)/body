{
  if (myDeleteBookUidsStatement == null) {
    myDeleteBookUidsStatement=myDatabase.compileStatement("DELETE FROM BookUid WHERE book_id = ?");
  }
  myDeleteBookUidsStatement.bindLong(1,bookId);
  myDeleteBookUidsStatement.execute();
}
