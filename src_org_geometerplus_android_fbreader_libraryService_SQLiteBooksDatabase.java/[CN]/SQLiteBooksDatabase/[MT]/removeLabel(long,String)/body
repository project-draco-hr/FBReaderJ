{
  final SQLiteStatement statement=get("DELETE FROM BookLabel WHERE book_id=? AND label_id IN" + " (SELECT label_id FROM Labels WHERE name=?)");
  statement.bindLong(1,bookId);
  statement.bindString(2,label);
  statement.execute();
}
