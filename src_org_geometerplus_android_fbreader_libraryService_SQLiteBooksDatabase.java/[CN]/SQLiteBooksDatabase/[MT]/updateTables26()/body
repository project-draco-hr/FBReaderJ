{
  myDatabase.execSQL("CREATE TABLE IF NOT EXISTS BookSynchronizationInfo(" + "book_id INTEGER PRIMARY KEY NOT NULL UNIQUE REFERENCES Books(book_id)," + "synchronization_time INTEGER NOT NULL,"+ "hash TEXT(40) NOT NULL)");
}
