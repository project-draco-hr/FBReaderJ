{
  final int width=myWidth.getValue();
  final int height=myHeight.getValue();
  return getActionByZone(Math.max(0,Math.min(width,(int)width * x)),Math.max(0,Math.min(height,(int)height * y)),tap);
}
