{
  final FileChooserPreference preference=new FileChooserSinglePreference(myContext,rootResource,resourceKey,option,myBaseRequestCode + myPreferences.size(),onValueSetAction);
  myPreferences.add(preference);
  return preference;
}
